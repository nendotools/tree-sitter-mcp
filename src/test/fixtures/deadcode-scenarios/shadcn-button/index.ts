// Typical shadcn-ui barrel file that exports everything
export { Button, buttonVariants, type ButtonProps, type ButtonVariants } from './button'

// These exports exist but are never imported anywhere else
export { Badge } from './badge'  // This file doesn't exist
export { Card, CardHeader, CardContent } from './card'  // This file doesn't exist