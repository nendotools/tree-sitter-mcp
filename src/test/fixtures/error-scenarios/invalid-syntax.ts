// File with intentional syntax errors for error handling testing

export function invalidFunction( {
  // Missing parameter type and closing parenthesis
  const result = 
  // Missing assignment value
  
  if (someCondition {
    // Missing closing parenthesis
    return undefined
  }
  
  // Missing closing brace

export class IncompleteClass {
  public method(): string
    // Missing opening brace and function body